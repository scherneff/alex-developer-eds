export default async function decorate(block) {
  // Row 1 = background image, Row 2 = content (text + side image)
  // Structure is already correct from the authored HTML.
  // No additional DOM restructuring needed.
}
